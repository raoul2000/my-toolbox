const { spawn } = require('child_process');

/**
 * Run an external command
 * 
 * @param {string} cmdTemplate the command template string to execute
 * @param {hash} values object values
 */
exports.run = function(cmdTemplate, values) {

}